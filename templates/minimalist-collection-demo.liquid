---
title: Minimalist Product Cards Demo
layout: theme
---
<div class="page-width">
  <div class="section-header">
    <h1 class="section-header__title">Minimalist Product Cards</h1>
    <p class="section-header__subtitle">Clean, modern product display</p>
  </div>

  <div class="minimalist-product-grid">
    {% for product in collections.all.products limit: 8 %}
      <div class="minimalist-product-grid__item">
        {% render 'minimalist-product-card', product: product %}
      </div>
    {% endfor %}
  </div>
</div>

<style>
  .section-header {
    text-align: center;
    margin-bottom: 40px;
    padding: 60px 0 40px;
  }

  .section-header__title {
    font-size: 32px;
    font-weight: 700;
    margin: 0 0 8px 0;
    color: #1a1a1a;
  }

  .section-header__subtitle {
    font-size: 16px;
    color: #666;
    margin: 0;
  }

  .minimalist-product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 24px;
    margin-bottom: 60px;
  }

  .minimalist-product-grid__item {
    display: flex;
  }

  @media screen and (max-width: 768px) {
    .section-header {
      padding: 40px 0 20px;
    }

    .section-header__title {
      font-size: 24px;
    }

    .minimalist-product-grid {
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 16px;
      margin-bottom: 40px;
    }
  }
</style>
